<aside class="md:col-span-3 p-4 bg-white rounded-sm shadow">
  <nav class="divide-y-2 divide-gray-200 sticky top-4">

    <section class="pb-3">
      {% set navPages = collections.all | eleventyNavigation("Home") %}
      <ul class="space-y-1">
        {%- for entry in navPages %}
          <li class="">
            <a class="block rounded-sm py-2 px-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}" href="{{ relative }}{{ entry.url }}">{{ entry.title }}</a>
          </li>
        {%- endfor %}
      </ul>
    </section>

    <section class="py-3">
      <h3 class="px-4 py-2 uppercase font-bold text-sm">Toon, keel ja vorm</h3>

      {% set navPages = collections.all | eleventyNavigation("Language") %}
      <ul class="space-y-1">
        {%- for entry in navPages %}
          <li>
            <a class="block rounded-sm py-2 px-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}" href="{{ relative }}{{ entry.url }}">{{ entry.title }}</a>
          </li>
        {%- endfor %}
      </ul>
    </section>

    <section class="py-3">
      <h3 class="px-4 py-2 uppercase font-bold text-sm">Kasutajaliides ja -kogemus</h3>

      {% set navPages = collections.all | eleventyNavigation("Design") %}
      <ul class="space-y-1">
        {%- for entry in navPages %}
          <li>
            <a class="block rounded-sm py-2 px-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}" href="{{ relative }}{{ entry.url }}">{{ entry.title }}</a>
          </li>
        {%- endfor %}
      </ul>
    </section>

    <section class="py-3">
      <h3 class="px-4 py-2 uppercase font-bold text-sm">Ikoonid ja logod</h3>
      {% set navPages = collections.all | eleventyNavigation("Icons") %}
      <ul class="space-y-1">
        {%- for entry in navPages %}
          <li>
            <a class="block rounded-sm py-2 px-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}" href="{{ relative }}{{ entry.url }}">{{ entry.title }}</a>
          </li>
        {%- endfor %}
      </ul>
    </section>

    <section class="py-3">
      <h3 class="px-4 py-2 uppercase font-bold text-sm">Klassid ja omadused</h3>
      {% set navPages = collections.all | eleventyNavigation("Classes") %}
      <ul class="space-y-1">
        {%- for entry in navPages %}
          <li>
            <a class="block rounded-sm py-2 px-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}" href="{{ relative }}{{ entry.url }}">{{ entry.title }}</a>
          </li>
        {%- endfor %}
      </ul>
    </section>

    <section class="py-3">
      <h3 class="px-4 py-2 uppercase font-bold text-sm">Komponendid ja küljendid</h3>

      {% set navPages = collections.all | eleventyNavigation("Components") %}
      <ul class="mb-4 space-y-0.5">
        {%- for entry in navPages %}
          <li>
            <a
              class="block rounded-sm py-2 pl-6 pr-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}"
              href="{{ relative }}{{ entry.url }}">{{ entry.title }}
            </a>
          </li>
        {%- endfor %}
      </ul>

      <h3 class="px-4 py-2 uppercase text-sm border-b border-dashed">Navigeerimine</h3>
      {% set navPages = collections.all | eleventyNavigation("Navigation") %}
      <ul class="mb-4 space-y-0.5">
        {%- for entry in navPages %}
          <li>
            <a
              class="block rounded-sm py-2 pl-6 pr-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}"
              href="{{ relative }}{{ entry.url }}">{{ entry.title }}
            </a>
          </li>
        {%- endfor %}
      </ul>

      <h3 class="px-4 py-2 uppercase text-sm border-b border-dashed">Vorm</h3>
      {% set navPages = collections.all | eleventyNavigation("Form") %}
      <ul class="mb-4 space-y-0.5">
        {%- for entry in navPages %}
          <li>
            <a
              class="block rounded-sm py-2 pl-6 pr-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}"
              href="{{ relative }}{{ entry.url }}">{{ entry.title }}
            </a>
          </li>
        {%- endfor %}
      </ul>

      <h3 class="px-4 py-2 uppercase text-sm border-b border-dashed">Tagasiside</h3>
      {% set navPages = collections.all | eleventyNavigation("Feedback") %}
      <ul class="mb-4 space-y-0.5">
        {%- for entry in navPages %}
          <li>
            <a
              class="block rounded-sm py-2 pl-6 pr-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}"
              href="{{ relative }}{{ entry.url }}">{{ entry.title }}
            </a>
          </li>
        {%- endfor %}
      </ul>

      <h3 class="px-4 py-2 uppercase text-sm border-b border-dashed">Näited</h3>
      {% set navPages = collections.all | eleventyNavigation("Examples") %}
      <ul class="mb-4 space-y-0.5">
        {%- for entry in navPages %}
          <li>
            <a
              class="block rounded-sm py-2 pl-6 pr-4 hover:bg-green-50 {% if entry.url == page.url %} bg-green-50 {% endif %}"
              href="{{ relative }}{{ entry.url }}">{{ entry.title }}
            </a>
          </li>
        {%- endfor %}
      </ul>
    </section>

  </nav>
</aside>